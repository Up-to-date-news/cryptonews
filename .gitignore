node_modules/
.env
dist/
*.log

# Regenerated by web/scripts/copy-data.js from the real /data at the repo
# root before each dev/build — never the source of truth, never committed.
web/public/data/
web/public/sitemap.xml
